/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import PageContainer from './src/Pages/MainPage';
AppRegistry.registerComponent(appName, () => PageContainer);
